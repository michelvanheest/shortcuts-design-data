<!DOCTYPE html>
<html lang="en-us">
    <head>
        <link rel="stylesheet" href="/css/toast.css">
    </head>
    <body>
        {% set sponsorlist = collections.sponsors %}
        {% for sponsor in sponsorlist %}
            {% if sponsor.data.active == "yes" %}
                <div id="toast-wrapper">
                    {# <a href="https://paypal.me/michelvanheest?ref=shortcuts.design" id="toast" target="_blank" rel="noopener">Buy me a coffee ☕❤️</a> #}
                    {# <a href="#link" id="this-months-sponsor-toast">Proudly sponsored by {{ sponsor.data.sponsorName }} {{ sponsor.data.sponsorEmoji }}</a> #}
                    <a href="#link" id="this-months-sponsor-toast">Design calculators, forms, and more without code with {{ sponsor.data.sponsorName }}, and embed them in Framer.</a>
                </div>
            {% endif %}
        {% endfor %}
        <script type="text/javascript" src="/js/animate.js"></script>
    </body>
</html>